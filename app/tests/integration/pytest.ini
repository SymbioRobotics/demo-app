[pytest]
# TODO Enable this once other build issues have stabilized.
# addopts = -p docker_compose --docker-compose tests/integration/docker-compose.

junit_family = xunit1
junit_suite_name = Reference App Integration Tests
